# Decap CMS — Ellen Carty site
# Update backend.repo + base_url after GitHub OAuth proxy is live.

backend:
  name: github
  repo: Squarebell/ellen-in-politics
  branch: main
  base_url: https://ellen-decap-oauth.squarebell.workers.dev
  auth_endpoint: auth

# Update site_url / display_url when custom domain is attached
site_url: https://ellen-in-politics.squarebell.workers.dev
logo_url: /favicon.ico
display_url: https://ellen-in-politics.squarebell.workers.dev
search: false

# Uploaded images land in the repo and ship with the next deploy
media_folder: public/uploads
public_folder: /uploads

collections:
  - name: posts
    label: Writing
    label_singular: Post
    folder: content/posts
    create: true
    delete: true
    slug: "{{slug}}"
    extension: md
    format: frontmatter
    sortable_fields: ["date", "title", "topic"]
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Excerpt, name: excerpt, widget: text }
      - { label: Publish date, name: date, widget: datetime, date_format: YYYY-MM-DD, time_format: false, format: YYYY-MM-DD }
      - {
          label: Topic,
          name: topic,
          widget: select,
          options: ["Civic", "Feminism", "Democracy", "Career", "Policy"],
          default: "Civic",
        }
      - { label: Cover image URL, name: image, widget: string, required: false }
      - { label: Cover image alt, name: imageAlt, widget: string, required: false }
      - { label: Body, name: body, widget: markdown }
